# Log de build — subagentes

Anexado automaticamente pelo hook `log-subagent.mjs` (evento `SubagentStop`). Cada linha registra
modelo usado, faixa e resultado — é o registro de rastreabilidade e custo do projeto (seção
5.11.2 do prompt único).

| Quando | Subagente | Modelo | Resultado |
|---|---|---|---|
