{
  "name": "@titan/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Processo persistente, NÃO serverless (seção 5.5 do prompt único): jobs BullMQ, canais, fiscal (cert A1), pricing, agentes.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@titan/domain": "workspace:*",
    "bullmq": "^5.34.4",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  }
}
