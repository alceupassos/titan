{
  "name": "@titan/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Servidor MCP próprio (seção 9.12.4 do prompt único). Fase 0: stub titan-mcp-dev. titan-mcp-prod (catálogo restrito, consumido só pelo Hermes) nasce na Fase 10 — nunca confundir as duas instâncias.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  }
}
